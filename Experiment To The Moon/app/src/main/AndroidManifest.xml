<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.Experiment_To_The_Moon">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExperimentToTheMoon"
        tools:ignore="AllowBackup">
        <activity android:name=".SearchActivity" />
            <!-- android:parentActivityName=".MainActivity"></activity> -->
        <activity
            android:name=".DisplayUserProfile"
            android:label="@string/title_activity_display_user_profile"
            android:theme="@style/Theme.ExperimentToTheMoon.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ExperimentActivity" />
            <!--android:parentActivityName=".MainActivity"/> -->
        <activity android:name=".Question"></activity>
        <activity android:name=".Answer"></activity>

    </application>

</manifest>